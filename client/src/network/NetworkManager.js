// NetworkManager.js
export class NetworkManager {
  constructor() {
    // Simule juste un objet réseau pour les autres classes
    this.isConnected = true;
    this.sessionId = 'MOCK123';
    this.currentZone = null;
  }
  // Pas d’implémentation
}
